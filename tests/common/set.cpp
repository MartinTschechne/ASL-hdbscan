#include "gtest/gtest.h"
#include <common/set.h>

TEST(set, set_init_free) {
    // list* l = list_create();
    // ASSERT_EQ(l->head, nullptr);
    // ASSERT_EQ(l->tail, nullptr);

    // list_free(l);
}    
